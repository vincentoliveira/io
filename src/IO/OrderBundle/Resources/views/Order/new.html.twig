{% extends "IOCarteBundle::layout.html.twig" %}

{% block content %}
    <h3>Ajouter une commande</h3>

    {{ form_start(form) }}
        <div class="col-md-6">
            <ul class="orderLines" data-prototype="{{ form_widget(form.orderLines.vars.prototype)|e }}">
                {% for orderLines in form.orderLines %}
                    <li>
                        {{ form_row(orderLines) }}
                        {{ orderLines.vars.name }}
                        {#<a class="btn btn-danger btn-delete-dish">x</a>#}
                    </li>
                {% endfor %}
            </ul>
        </div>
        <div class="col-md-6">
            <div class="form-group">
                {{ form_row(form.tableName) }}
            </div>
            <div class="form-group">
                {{ form_row(form.status) }}
            </div>
        </div>
        <div class="clearfix"></div>
        
        {{ form_rest(form) }}
    {{ form_end(form) }}
        
    <h3>Plats</h3>
    {% for category in categories %}
        <h5>{{ category.name }}</h5>
        {% for dish in category.dishes %}
            <a class="btn btn-default btn-dish" href="#" data-id="{{ dish.id }}" data-price="{{ dish.price }}"  data-name="{{ dish.name}}">
                <img src="{{ dish.media.thumbnailPath|imageLink }}" alt="#" />
                {{ dish.name }}
            </a>
        {% endfor %}
    {% endfor %}
    
{% endblock %}
