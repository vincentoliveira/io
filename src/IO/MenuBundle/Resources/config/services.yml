parameters:
#    io_menu.example.class: IO\MenuBundle\Example

services:
    menu.category:
        class: IO\MenuBundle\Service\CategoryService
        arguments: ["@doctrine.orm.entity_manager"]
        
    menu.dish:
        class: IO\MenuBundle\Service\DishService
        arguments: ["@doctrine.orm.entity_manager"]

